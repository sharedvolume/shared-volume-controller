apiVersion: sv.sharedvolume.io/v1alpha1
kind: SharedVolume
metadata:
  name: test-phase-progression
  namespace: default
spec:
  mountPath: "/data"
  storage:
    capacity: "1Gi"
    accessMode: "ReadWrite"
  storageClassName: "nfs-csi"
