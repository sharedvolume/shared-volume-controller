apiVersion: sv.sharedvolume.io/v1alpha1
kind: SharedVolume
metadata:
  name: fail-name-too-long-sv-13-with-a-very-long-name-that-exceeds-kubernetes-dns-label-limits-which-should-be-rejected
  namespace: {{TEST_NAMESPACE_1}}
spec:
  mountPath: "{{MOUNT_PATH_FAIL_NAME}}"
  syncInterval: "{{SYNC_INTERVAL_SHORT}}"
  syncTimeout: "{{SYNC_TIMEOUT_SHORT}}"
  storageClassName: "{{STORAGE_CLASS}}"
  storage:
    capacity: "{{STORAGE_CAPACITY_SMALL}}"