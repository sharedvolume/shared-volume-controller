apiVersion: sv.sharedvolume.io/v1alpha1
kind: SharedVolume
metadata:
  name: fail-timeout-gt-interval-sv-14
  namespace: {{TEST_NAMESPACE_1}}
spec:
  mountPath: "{{MOUNT_PATH_FAIL_TIMEOUT}}"
  syncInterval: "{{SYNC_TIMEOUT_SHORT}}"
  syncTimeout: "{{SYNC_TIMEOUT_MEDIUM}}"
  storageClassName: "{{STORAGE_CLASS}}"
  storage:
    capacity: "{{STORAGE_CAPACITY_SMALL}}"